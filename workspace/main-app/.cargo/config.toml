[target.riscv32imc-unknown-none-elf]
runner = "probe-rs run --chip=esp32c3 --preverify --always-print-stacktrace --no-location --catch-hardfault"

[build]
target = "riscv32imc-unknown-none-elf"
rustflags = ["-C", "force-frame-pointers"]

[unstable]
build-std = ["core"]

[env]
# WiFi configuration for pilot deployment
# UPDATE THESE VALUES FOR YOUR NETWORK BEFORE DEPLOYMENT
WIFI_SSID = "FamiliaFeliz-2Ghz"
WIFI_PASSWORD = "ines#sara"

# MQTT broker configuration for pilot deployment  
# UPDATE THESE VALUES FOR YOUR BROKER BEFORE DEPLOYMENT
MQTT_BROKER_IP = "10.10.10.210"
MQTT_BROKER_PORT = "1883"
MQTT_CLIENT_ID = "esp32-c3-pilot"
MQTT_TOPIC_PREFIX = "iot-pilot"
